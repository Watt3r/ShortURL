<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^([^\.\/]*)$ decoder.php?decode=$1 [L]
</IfModule>
# Internally rewrite shortened URL requests to de-shortened URL lookup script filepath plus query string
